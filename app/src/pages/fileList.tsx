import * as React from "react";

import FileList from "../components/fileList";

const Component = () => {
  console.log('fileList.tsx page loading');
  return (
    <FileList/>
  );
};

export default Component;
